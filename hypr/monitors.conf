# See https://wiki.hyprland.org/Configuring/Monitors/
# List current monitors and resolutions possible: hyprctl monitors
# Format: monitor = [port], resolution, position, scale

# Optimized for retina-class 2x displays, like 13" 2.8K, 27" 5K, 32" 6K.
# env = GDK_SCALE,2
# monitor=,preferred,auto,auto

# Per-monitor scaling based on physical DPI
# GDK_SCALE removed - Wayland-native apps (Chrome, Electron) read scale from compositor
monitor = eDP-1, 1920x1080@60, 0x0, 1.5           # Laptop: higher DPI (~132), keep 1.5x
monitor = HDMI-A-1, 1920x1080@60, 1280x0, 1.0     # External: lower DPI (~92), use 1.0x

# Good compromise for 27" or 32" 4K monitors (but fractional!)
# env = GDK_SCALE,1.75
# monitor=,preferred,auto,1.6

# Straight 1x setup for low-resolution displays like 1080p or 1440p
# env = GDK_SCALE,1
# monitor=,preferred,auto,1

# Example for Framework 13 w/ 6K XDR Apple display
# monitor = DP-5, 6016x3384@60, auto, 2
# monitor = eDP-1, 2880x1920@120, auto, 2
